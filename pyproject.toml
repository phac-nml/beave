
[build-system]
requires = ["scikit-build-core >=0.4.3", "nanobind >=1.3.2"]
build-backend = "scikit_build_core.build"

[project]
name = "dist-mat"
description = "A small utility fast clustering"
readme = "README.md"
requires-python = ">=3.13"
authors = [{ name = "Matthew Wells", email = "matthew.wells@phac-aspc.gc.ca" }]
classifiers = ["License :: Apache-2.0"]
dependencies = ["numpy >= 2.4.0", "polars >=1.38.0", "scipy >= 1.17.0"]
version = "0.0.6"

[project.scripts]
dist-mat = "dist_mat.__main__:main"

[project.optional-dependencies]
dev = [
  "pytest>=9.0.2",
  "scipy-stubs",
  "hypothesis>=6.0.0",
  "pytest-workflow>=2.0.1",
  "mypy>=1.19.1",
  "ruff>=0.15.7",
  "pytest-benchmark>=5.2.3",
]

[tool.scikit-build]
minimum-version = "0.8"
build-dir = "build/{wheel_tag}"
wheel.py-api = "cp312" # build wheels for stable API Cpython 3.12+
cmake.args = ["-DTARGET_GROUP=python"]
cmake.version = ">=3.26.1"
messages.after-success = "{green}Wheel successfully built"
messages.after-failure = """
{bold.red}Sorry{normal}, build failed. Your platform is {platform.platform}.
"""
logging.level = "DEBUG"


[tool.mypy]
exclude = ["^test", "src/dist-mat/test\\.py$"]
files = ["src/dist_mat/*.py"]
disallow_untyped_defs = true
disallow_incomplete_defs = true


[tool.complexipy]
paths = ["src/dist_mat"]
max-complexity-allowed = 10


[tool.ruff]
line-length = 100
lint.select = [
  "E",
  "F",
  "TRY",
  "FLY",
  "I",
  "NPY",
  "N",
  "PERF",
  "W",
  "D",
  "PL",
  "PLE",
  "PLR",
  "PLW",
  "UP",
  "FURB",
  "RUF",
]
